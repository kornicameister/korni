collectors:

- type: js-npm
  path: /
  actors:
  - type: js-npm
    versions: "L.Y.Y"
    settings:
      github_labels:
      - dependency
      - update
  - type: repo-issue
    versions: "Y.0.0"
    settings:
      github_labels:
      - dependency
      - update
